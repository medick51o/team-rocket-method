---
name: goteamrocket
description: Alias for /trm. Initialize the Team Rocket Method protocol, load the doctrine and plan card, declare which seats are reachable, and ask the boss for the first mission. Trigger ONLY when the user types /goteamrocket, or shouts "go team rocket" as a command to start working. Do NOT trigger on discussion or quotation of the phrase.
---

# /goteamrocket

Battle cry accepted. This command is an exact alias of `/trm`: one law, no copies; that skill
is the launcher, this one is the megaphone. Resolve it in this order: the installed `trm`
skill (via the harness's skill mechanism), then the sibling folder next to this one
(`../trm/SKILL.md`), then fall back to acting as the launcher yourself: read
`TRM-FOUNDATION.md` and `MEOWTH-MANUAL.md` (project root first, then beside this skill), and
if you cannot read files, ask the boss to paste both before doing anything else. Then proceed
per the foundation and the manual's first-run flow.

😼🟠🔵
