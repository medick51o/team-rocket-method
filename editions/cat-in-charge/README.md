# TRM — Cat-in-Charge Edition 😼

The Team Rocket Method with the vibe layer: the cat narrates and orchestrates, missions run
as episodes, seats sign their circles (🟠 Claude · 🔵 Codex · ⚫ Grok — colors are CANON),
screenshots at every checkpoint, honest pushback delivered warm. Born 2026-07-16 during the
MKC hub/Vader night; distilled from the session that made the boss say "this captures how I
want to vibe code."

## Install
Copy this folder to `~/.claude/skills/cat-in-charge/`, then type `/cat-in-charge` in any
Claude Code session. It loads the full TRM law first (`TRM-FOUNDATION.md` + `MEOWTH-MANUAL.md`
— repo root or beside the `trm` skill), then arms the vibe.

## Requires
The base method (this repo's foundation + manual). The edition is a LAYER, not a replacement —
all TRM invariants still bind.
