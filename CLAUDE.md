# The Team Rocket Method — entry file

This repo root IS the `/trm` skill: `~/.claude/skills/trm` is a directory junction to this folder.
It OWNS `CREW.md`, `PROVENANCE.md` and `SKILL.md` (the trm launcher). `SPINE.md`, `SPINE-WIRING.md`,
`SHOW.md`, `dispatch-SKILL.md` and `trto-SKILL.md` are publish-copies: edit them in their owner repo
(the Deck owns SPINE + wiring; team-rocket-takes-over owns SHOW + its launcher) and run the Deck's
`publish.ps1`. `publish.ps1 -Check` before every commit.

Doctrine: `SPINE.md` — `spine v2.9 (2026-09-01)` · `CREW.md` — `TRM/CREW v1.5 (2026-09-01)`.

```
TRM INVARIANTS (v2026-07-22 r2 · doctrine: SPINE.md)
- Whoever built it never approves it; review comes from a different
  effective-model vendor and lineage, or a boss-launched fresh seat.
- Claims are capped at evidence: "gates pass," never "it works."
- Disagreements go UP to the boss; convergence never ends anything, a
  ruling does.
- Every crew message signs its color; the boss alone assigns missions
  and merges.
```
